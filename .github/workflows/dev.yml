name: Dev Pipeline

on:
  push:
    branches: ['dev']  
  workflow_dispatch:


jobs:
  dev-sync-deploy-verify-job:   
    uses: ./.github/workflows/sync-deploy-verify.yml
    with:
      BRANCH: dev
      PIPELINEID: ${{ secrtes.DEV_PIPELINEID }}
      SHOULD_TRIGGER_PIPELINE: false
